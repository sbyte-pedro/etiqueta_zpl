meta {
  name: List Versions
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/designs/{{designId}}/versions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
