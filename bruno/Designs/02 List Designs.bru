meta {
  name: List Designs
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/designs
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
